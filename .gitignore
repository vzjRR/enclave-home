node_modules/
*.log
.env
.DS_Store

# Runtime state. news.json is written by the admin console at run time and
# belongs to the deployment, not the repository — committing it would let a
# deploy overwrite live posts with whatever was in the last commit.
data/*.json
data/*.tmp
